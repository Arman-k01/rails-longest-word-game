<!DOCTYPE html>
<html>
  <head>
    <title>Longest Word Game</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  </head>
  <body>
    <div class="container">
      <h1 class="text-center my-4">Longest Word Game</h1>
      <div class="text-center">
        <h3>Letters Grid</h3>
        <ul class="list-inline">
          <% @letters.each do |letter| %>
            <li class="list-inline-item border rounded p-2"><%= letter %></li>
          <% end %>
        </ul>
        <%= form_with url: score_game_path, method: :post, local: true, class: "mt-4" do |form| %>
          <%= hidden_field_tag :letters, @letters.join(" ") %>
          <div class="form-group">
            <%= form.label :word, "Enter a word:" %>
            <%= form.text_field :word, class: "form-control" %>
          </div>
          <%= form.submit "Submit", class: "btn btn-primary mt-2" %>
        <% end %>
      </div>
    </div>
  </body>
</html>
